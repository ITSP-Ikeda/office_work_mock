<%= form_with(model: parking) do |form| %>
  <% if parking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parking.errors.count, "error") %> prohibited this parking from being saved:</h2>

      <ul>
        <% parking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :parking_name, style: "display: block" %>
    <%= form.text_field :parking_name %>
  </div>

  <div>
    <%= form.label :payment_destination_id, style: "display: block" %>
    <%= form.collection_select :payment_destination_id, PaymentDestination.all, :id, :payment_destination_name %>
    </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
